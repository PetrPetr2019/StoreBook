<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Интернет магазин  книг на С#</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
<header>
    <nav nav class="navbar navbar-expand-sm navbar-light">
        <a class="navbar-brand"asp-area="" asp-controller="Home" asp-action="Index">Интернет магазин книг на С# </a>
        <form class="form-inline my-2 my-lg-0" action="@Url.Action("Index", "Search")" method="get">
            <input class="form-control mr-sm-2" type="search" name="query" placeholder="автор или название"/>
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Искать</button>
            @*<a class="navbar-brand" @Url.Action("Index", "Search")>Пример альтернатива</a>*@

        </form>
        <span class="navbar-text ml-auto">
            @{
                Cart cart;
                if(!Context.Session.TryGetCart(out cart))
                {
                    cart = new Cart();
                }

                if(cart.Items.Count==0)
                {
                    @:Корзина пуста
                }
                else
                {
                    @:Товар @cart.Items.Count на сумму @cart.Amount
                }
            }
        </span>
    </nav>
</header>
<div class="container">
    <main>
        @RenderBody()
    </main>
</div>
</body>
</html>







